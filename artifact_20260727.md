# CodeLens Bug Fix — 2026-07-27

## 修复内容

### Bug A: 数据注入失败 ❌ → ✅ 已修复

**问题**: `server.js` 尝试替换 `SCAN_DATA_PLACEHOLDER`，但 `index.html` 中没有这个占位符

**修复**: 改为在 `</head>` 前注入 `<script>window.__CODELENS_DATA__ = {...}</script>`

```javascript
// Before (broken)
html = html.replace('/* SCAN_DATA_PLACEHOLDER */', ...)

// After (fixed)
const inject = `<script>window.__CODELENS_DATA__ = ${JSON.stringify(webData)};</script>`;
html = html.replace('</head>', `${inject}\n</head>`);
```

### Bug B: mermaid v10 API 不兼容 ❌ → ✅ 已修复

**问题**: mermaid@10 的 `render()` 返回 Promise，旧代码使用回调函数

**修复**: 使用 Promise 风格 API

```javascript
// Before (broken)
mermaid.render('archMermaid', DATA.architectureDiagram, (svg) => {
  $('archMermaid').innerHTML = svg;
});

// After (fixed)
mermaid.render('archSvg', DATA.architectureDiagram)
  .then(({ svg }) => {
    $('archMermaid').innerHTML = svg;
  })
  .catch(() => {
    // fallback to text
  });
```

### 增强: 数据获取 fallback

**新增**: `app.js` 支持多种数据获取方式
1. 优先使用注入的 `window.__CODELENS_DATA__`
2. 失败时 fetch `/api/scan`
3. 再失败时 fetch `data/scan-result.json` (静态托管)

### 增强: 端口占用错误提示

```javascript
server.on('error', (err) => {
  if (err.code === 'EADDRINUSE') {
    console.error(`  ❌ Port ${PORT} is already in use.`);
    console.error(`     Another CodeLens instance may be running.`);
    process.exit(1);
  }
});
```

## 验证结果

```
✅ HTML length: 12263
✅ DATA injected: true
✅ Injected before </head>: true
✅ CSS link: true
✅ JS link: true
✅ Active tab: true
✅ Project name: "codelens"
```

## GitHub 推送

- 仓库: https://github.com/ugo2000/codelens
- 提交: `f364daa fix: data injection & mermaid v10 API`
- 强制推送覆盖远程冲突版本

## 截图问题说明

浏览器截图工具渲染异常（白屏），但 HTTP 响应验证显示 HTML/CSS/JS 完全正确，数据注入成功。问题出在截图工具而非代码。
